# 보고일 날짜 네비게이션 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 모바일 편집 폼의 보고일 카드에 ‹ / › 버튼(7일 이동)과 "이번 주일" 버튼을 추가해 날짜 선택을 편리하게 한다. 데스크탑은 기존 네이티브 input 유지.

**Architecture:** `src/styles.css` base 블록에 `.date-nav-*` 스타일을 추가하고 데스크탑 미디어쿼리에서 nav 숨김/native 표시를 오버라이드한다. `TabbedReportForm.tsx`에 날짜 헬퍼 함수 3개와 nav UI JSX를 추가하며, 기존 `<input type="date">`는 `className="date-native-input"`으로 변경해 데스크탑에서만 보이게 한다.

**Tech Stack:** CSS (src/styles.css), React 19 TSX (src/features/report/TabbedReportForm.tsx)

---

### Task 1: styles.css — date-nav 스타일 추가

**Files:**
- Modify: `src/styles.css`

> CSS 변경이므로 빌드 통과를 검증 기준으로 한다.

- [ ] **Step 1: base 블록에 date-nav 스타일 추가**

`src/styles.css`에서 아래를 찾아:

```css
/* ── Attendance Summary Stats ── */
/* ── 출결 통계 (bar-chart / report-canvas 스타일) ─────────── */
```

그 **바로 위**에 아래 블록 삽입:

```css
/* ── 날짜 네비게이션 (모바일 기본값, 데스크탑은 미디어쿼리에서 숨김) ── */
.date-nav-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 2px;
}

.date-nav-btn {
  background: transparent;
  border: none;
  color: var(--clr-primary);
  font-size: 20px;
  font-weight: 700;
  padding: 0 2px;
  line-height: 1;
  cursor: pointer;
  flex-shrink: 0;
}

.date-nav-display {
  font-size: 13px;
  font-weight: 700;
  color: var(--clr-primary);
  text-align: center;
  flex: 1;
  min-width: 0;
  white-space: nowrap;
}

/* 모바일: 네이티브 date input 숨김 */
.date-native-input {
  display: none;
}

/* 이번 주일 버튼 (모바일 전용) */
.date-this-sunday-btn {
  width: 100%;
  padding: 8px;
  border: 1px solid var(--clr-border-soft);
  border-radius: 8px;
  background: transparent;
  color: var(--clr-primary);
  font-size: 13px;
  font-weight: 700;
  cursor: pointer;
  text-align: center;
}

```

- [ ] **Step 2: 데스크탑 미디어쿼리에 nav 숨김 + native 표시 추가**

`src/styles.css`에서 아래를 찾아:

```css
  /* 기본정보 헤더는 모바일 전용 */
  .info-fields-heading {
    display: none;
  }
```

그 **바로 아래**에 추가:

```css

  /* 날짜 네비게이션은 모바일 전용 */
  .date-nav-row,
  .date-this-sunday-btn {
    display: none;
  }

  /* 데스크탑: 네이티브 date input 표시 복원 */
  .date-native-input {
    display: block;
  }
```

(이 블록은 `@media (min-width: 821px) and (pointer: fine)` 안에 들어간다.)

- [ ] **Step 3: 모바일 섹션 — info-fields-row margin 조정 + date-this-sunday-btn margin 추가**

`src/styles.css`에서 아래를 찾아:

```css
  /* 보고일/제목 카드 → 부서별보고 섹션 사이 여백 */
  .mobile-editor-screen .info-fields-row {
    margin-bottom: 16px;
  }
```

아래로 교체:

```css
  /* 보고일/제목 카드 → 이번주일 버튼 사이 여백 */
  .mobile-editor-screen .info-fields-row {
    margin-bottom: 8px;
  }

  /* 이번 주일 버튼 → 부서별보고 섹션 사이 여백 */
  .mobile-editor-screen .date-this-sunday-btn {
    margin-bottom: 16px;
  }
```

- [ ] **Step 4: 빌드 확인**

```bash
cd "/Users/kimwoojung/Documents/New project/Projects/ministry-report-v2"
npm run build
```

Expected: `✓ built in ...ms` — 에러 없음

- [ ] **Step 5: 커밋**

```bash
git add src/styles.css
git commit -m "feat(ui): date-nav 스타일 추가 — 모바일 보고일 네비게이션"
```

---

### Task 2: TabbedReportForm.tsx — 날짜 네비게이션 UI + 헬퍼 함수

**Files:**
- Modify: `src/features/report/TabbedReportForm.tsx`

- [ ] **Step 1: 날짜 헬퍼 + 핸들러 함수 일괄 추가**

`src/features/report/TabbedReportForm.tsx`에서 아래를 찾아:

```typescript
  function updateReport(patch: Partial<MinistryReport>) {
    const now = new Date().toISOString();
    onChange({ ...report, ...patch, updatedAt: now });
  }
```

그 **바로 위**(= `updateReport` 앞)에 아래 블록 전체 삽입:

```typescript
  // ── 날짜 네비게이션 헬퍼 ──────────────────────────────────────
  function localDateStr(d: Date): string {
    // toISOString()은 UTC → KST 자정 근처 날짜 오류. 로컬 날짜 직접 포맷.
    const y = d.getFullYear();
    const m = String(d.getMonth() + 1).padStart(2, "0");
    const day = String(d.getDate()).padStart(2, "0");
    return `${y}-${m}-${day}`;
  }

  function formatReportDate(iso: string): string {
    // "2026-05-17" → "2026. 05. 17."
    const [y, mo, d] = iso.split("-");
    return `${y}. ${mo}. ${d}.`;
  }

  function shiftWeek(iso: string, delta: number): string {
    // 로컬 자정 기준으로 delta * 7일 이동
    const d = new Date(iso + "T00:00:00");
    d.setDate(d.getDate() + delta * 7);
    return localDateStr(d);
  }

  function getThisSunday(): string {
    // 오늘 기준 가장 최근 일요일 (오늘이 일요일이면 오늘)
    const d = new Date();
    d.setDate(d.getDate() - d.getDay()); // getDay() 0=일요일
    return localDateStr(d);
  }

  function handlePrevWeek() { updateReport({ reportDate: shiftWeek(report.reportDate, -1) }); }
  function handleNextWeek() { updateReport({ reportDate: shiftWeek(report.reportDate, +1) }); }
  function handleThisSunday() { updateReport({ reportDate: getThisSunday() }); }

```

- [ ] **Step 2: 보고일 label 내부 교체**

`src/features/report/TabbedReportForm.tsx`에서 아래를 찾아:

```tsx
            <label className="info-field-label">
              보고일
              <input
                type="date"
                value={report.reportDate}
                onChange={(e) => updateReport({ reportDate: e.currentTarget.value })}
              />
            </label>
```

아래로 교체:

```tsx
            <label className="info-field-label">
              보고일
              {/* 모바일: 날짜 네비게이션 (데스크탑은 CSS display:none) */}
              <div className="date-nav-row">
                <button
                  type="button"
                  className="date-nav-btn"
                  aria-label="이전 주"
                  onClick={handlePrevWeek}
                >‹</button>
                <span className="date-nav-display">{formatReportDate(report.reportDate)}</span>
                <button
                  type="button"
                  className="date-nav-btn"
                  aria-label="다음 주"
                  onClick={handleNextWeek}
                >›</button>
              </div>
              {/* 데스크탑: 네이티브 date input (모바일은 CSS display:none) */}
              <input
                className="date-native-input"
                type="date"
                value={report.reportDate}
                onChange={(e) => updateReport({ reportDate: e.currentTarget.value })}
              />
            </label>
```

- [ ] **Step 3: info-fields-row 바로 아래 "이번 주일" 버튼 추가**

`src/features/report/TabbedReportForm.tsx`에서 아래를 찾아:

```tsx
          {/* 보고일 / 제목 */}
          <div className="info-fields-row">
```

아래에서 `info-fields-row` **닫는 태그** (`</div>`) 다음을 찾아:

```tsx
          </div>
          {/* 출결 통계 카드 (데스크탑 전용) */}
```

아래로 교체:

```tsx
          </div>
          {/* 이번 주일 버튼 (모바일 전용, 데스크탑은 CSS display:none) */}
          <button
            type="button"
            className="date-this-sunday-btn"
            onClick={handleThisSunday}
          >
            이번 주일
          </button>
          {/* 출결 통계 카드 (데스크탑 전용) */}
```

- [ ] **Step 4: 빌드 확인**

```bash
cd "/Users/kimwoojung/Documents/New project/Projects/ministry-report-v2"
npm run build
```

Expected: `✓ built in ...ms` — TypeScript 에러 없음

- [ ] **Step 5: 커밋**

```bash
git add src/features/report/TabbedReportForm.tsx
git commit -m "feat(ui): 보고일 날짜 네비게이션 UI — ‹ 이전/다음 › + 이번 주일 버튼"
```

---

### Task 3: 버전 bump v2.7.5 + HANDOFF + 푸시

**Files:**
- Modify: `package.json`
- Modify: `.claude/HANDOFF.md`

- [ ] **Step 1: package.json 버전 변경**

`package.json`에서 `"version": "2.7.4"` → `"version": "2.7.5"`

- [ ] **Step 2: 빌드 재확인**

```bash
cd "/Users/kimwoojung/Documents/New project/Projects/ministry-report-v2"
npm run build
```

Expected: 성공

- [ ] **Step 3: HANDOFF.md 전체 교체**

`.claude/HANDOFF.md`를 아래 내용으로 교체:

```markdown
# ministry-report-v2 — Codex Handoff (v2.7.5)

## 현재 상태
- 브랜치: main
- 버전: 2.7.5

## 방금 수정한 내용

### 문제
모바일에서 보고일 `<input type="date">` 사용 시 OS 날짜 피커가 열려 주일 날짜를 빠르게 선택하기 불편했음.

### 해결

#### src/features/report/TabbedReportForm.tsx
- 날짜 헬퍼 함수 추가: `localDateStr`, `formatReportDate`, `shiftWeek`, `getThisSunday`
- 핸들러 추가: `handlePrevWeek` (-7일), `handleNextWeek` (+7일), `handleThisSunday` (이번 주일)
- 보고일 label 내부: 기존 `<input type="date">` → `<div class="date-nav-row">`(‹ 날짜 ›) + `<input class="date-native-input">` (데스크탑 전용)
- `info-fields-row` 아래 `<button class="date-this-sunday-btn">이번 주일</button>` 추가

#### src/styles.css
- base 블록: `.date-nav-row`, `.date-nav-btn`, `.date-nav-display` 스타일 추가
- base 블록: `.date-native-input { display: none }` (모바일 기본 숨김)
- base 블록: `.date-this-sunday-btn` 스타일 추가
- 데스크탑 미디어쿼리: `.date-nav-row, .date-this-sunday-btn { display: none }`, `.date-native-input { display: block }` 추가
- 모바일 섹션: `.info-fields-row { margin-bottom: 8px }`, `.date-this-sunday-btn { margin-bottom: 16px }` 추가

## 직전 주요 작업 (v2.7.4)
- 모바일 기본정보 섹션 정렬·간격 수정 (패딩 12px, 헤딩 margin-bottom: 10px)
- TabbedReportForm에 "기본정보" 섹션 헤더 추가

## 프로젝트 개요
- **프레임워크**: React 19 + Vite + TypeScript
- **스타일**: src/styles.css (단일 CSS 파일)
- **인증/DB**: Firebase Auth + Firestore + FCM
- **빌드**: `npm run build` (Vite + Workbox PWA)
- **개발 서버**: `npm run dev` (localhost:5173, SW 등록 없음)

## 주요 파일
- `src/styles.css` — 전체 스타일시트
- `src/features/report/TabbedReportForm.tsx` — 편집 폼 (기본정보 탭, 날짜 네비게이션)
- `src/features/report/MobileReportList.tsx` — 보고서 목록
- `src/App.tsx` — 앱 루트

## CSS 구조 주의사항
- base 스타일(미디어쿼리 없음): 모바일 기준값 — `.date-nav-*`은 여기서 표시
- `@media (min-width: 821px) and (pointer: fine)`: 데스크탑 — `.date-nav-*` 숨김, `.date-native-input` 표시
- `@media (max-width: 820px), (pointer: coarse)`: 모바일 추가 오버라이드

## 날짜 네비게이션 로직
- `shiftWeek(iso, delta)`: `new Date(iso + "T00:00:00")` 로컬 자정 기준, `delta * 7`일 이동
- `getThisSunday()`: `d.setDate(d.getDate() - d.getDay())` (getDay()=0이 일요일)
- `localDateStr(d)`: `toISOString()` 대신 로컬 날짜 직접 포맷 (KST 타임존 안전)

## 다음으로 할 수 있는 작업
- 기기 검증: 날짜 네비게이션 동작 확인
- 모바일 헤더 `--layer2-h: 36px` 실제 탭바 높이 기기 확인 후 조정

## 빌드 & 배포
```bash
npm run build
npm run dev         # localhost:5173
firebase deploy --project <project-id>
```
```

- [ ] **Step 4: 커밋 + 푸시**

```bash
git add package.json .claude/HANDOFF.md
git commit -m "chore: v2.7.5 — 보고일 네비게이션 완료, HANDOFF 갱신"
git push
```
