# Mobile UI 2.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 모바일 UI를 모던 카드 스타일로 전면 개편하고, 보고자/뷰어 앱 모드를 도입한다.

**Architecture:** 기존 `mode` 상태(`"edit"|"view"|"roster"`)에 `"account"`를 추가하고, 새 `appMode`(`"reporter"|"viewer"`)를 localStorage로 관리한다. 모바일에서만 하단 탭바·목록 홈 화면·그라데이션 헤더가 활성화된다. 데스크톱 레이아웃은 변경 없음.

**Tech Stack:** React 19, TypeScript, CSS custom properties (기존 테마 변수 유지), localStorage, Vitest

---

## 파일 구조

| 파일 | 역할 |
|---|---|
| `src/features/mode/useAppMode.ts` | 앱 모드(reporter/viewer) localStorage 훅 — 신규 |
| `src/features/mode/AppModeToggle.tsx` | 보고자/뷰어 전환 UI — 신규 |
| `src/features/nav/BottomTabBar.tsx` | 모바일 하단 탭바 — 신규 |
| `src/features/report/MobileReportList.tsx` | 모바일 목록 홈 화면 — 신규 |
| `src/features/auth/AuthGate.tsx` | 로그인 화면 리디자인 — 수정 |
| `src/styles.css` | 모바일 미디어쿼리 전면 개편 — 수정 |
| `src/App.tsx` | 모든 새 컴포넌트 통합, 상태 추가 — 수정 |
| `package.json` | version 2.0.0 — 수정 |

---

## Task 1: `useAppMode` 훅

**Files:**
- Create: `src/features/mode/useAppMode.ts`
- Create: `src/features/mode/useAppMode.test.ts`

- [ ] **Step 1: 테스트 작성**

```ts
// src/features/mode/useAppMode.test.ts
import { beforeEach, describe, expect, it } from "vitest";
import { readStoredMode } from "./useAppMode";

describe("readStoredMode", () => {
  beforeEach(() => localStorage.clear());

  it("returns 'reporter' when nothing stored", () => {
    expect(readStoredMode()).toBe("reporter");
  });

  it("returns 'viewer' when stored value is 'viewer'", () => {
    localStorage.setItem("ministry-app-mode", "viewer");
    expect(readStoredMode()).toBe("viewer");
  });

  it("returns 'reporter' for unknown stored value", () => {
    localStorage.setItem("ministry-app-mode", "garbage");
    expect(readStoredMode()).toBe("reporter");
  });
});
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```bash
cd "/Users/kimwoojung/Documents/New project/Projects/ministry-report-v2"
npm test -- --reporter=verbose src/features/mode/useAppMode.test.ts
```

Expected: FAIL — `readStoredMode` not found

- [ ] **Step 3: 구현**

```ts
// src/features/mode/useAppMode.ts
import { useState } from "react";

export type AppMode = "reporter" | "viewer";

const STORAGE_KEY = "ministry-app-mode";

export function readStoredMode(): AppMode {
  const stored = localStorage.getItem(STORAGE_KEY);
  return stored === "viewer" ? "viewer" : "reporter";
}

export function useAppMode(): [AppMode, (mode: AppMode) => void] {
  const [appMode, setAppModeState] = useState<AppMode>(readStoredMode);

  function setAppMode(mode: AppMode) {
    localStorage.setItem(STORAGE_KEY, mode);
    setAppModeState(mode);
  }

  return [appMode, setAppMode];
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
npm test -- --reporter=verbose src/features/mode/useAppMode.test.ts
```

Expected: 3 tests passing

- [ ] **Step 5: 커밋**

```bash
git add src/features/mode/useAppMode.ts src/features/mode/useAppMode.test.ts
git commit -m "feat: add useAppMode hook with localStorage persistence"
```

---

## Task 2: `AppModeToggle` 컴포넌트

**Files:**
- Create: `src/features/mode/AppModeToggle.tsx`
- Modify: `src/styles.css` (`.app-mode-toggle` CSS 추가)

- [ ] **Step 1: 컴포넌트 작성**

```tsx
// src/features/mode/AppModeToggle.tsx
import type { AppMode } from "./useAppMode";

type AppModeToggleProps = {
  appMode: AppMode;
  onAppModeChange: (mode: AppMode) => void;
};

export function AppModeToggle({ appMode, onAppModeChange }: AppModeToggleProps) {
  return (
    <div className="app-mode-toggle">
      <span className="app-mode-toggle-label">앱 모드</span>
      <div className="app-mode-toggle-buttons" role="group" aria-label="앱 모드 선택">
        <button
          type="button"
          className={`app-mode-btn${appMode === "reporter" ? " active" : ""}`}
          aria-pressed={appMode === "reporter"}
          onClick={() => onAppModeChange("reporter")}
        >
          ✏️ 보고자
        </button>
        <button
          type="button"
          className={`app-mode-btn${appMode === "viewer" ? " active" : ""}`}
          aria-pressed={appMode === "viewer"}
          onClick={() => onAppModeChange("viewer")}
        >
          👁 뷰어
        </button>
      </div>
      {appMode === "viewer" && (
        <p className="app-mode-hint">뷰어 모드: 보고서를 읽기 전용으로 봅니다</p>
      )}
    </div>
  );
}
```

- [ ] **Step 2: CSS 추가** — `src/styles.css` 끝에 추가

```css
/* ── AppModeToggle ── */
.app-mode-toggle {
  display: flex;
  flex-direction: column;
  gap: 8px;
  padding: 16px;
  background: var(--clr-card-bg);
  border-radius: 12px;
  border: 1px solid var(--clr-border);
}

.app-mode-toggle-label {
  font-size: 12px;
  font-weight: 700;
  color: var(--clr-text-muted);
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.app-mode-toggle-buttons {
  display: flex;
  gap: 0;
  background: var(--clr-surface);
  border-radius: 8px;
  padding: 3px;
}

.app-mode-btn {
  flex: 1;
  background: transparent;
  color: var(--clr-text-muted);
  border-radius: 6px;
  font-size: 13px;
  font-weight: 600;
  padding: 8px 12px;
  transition: background 0.15s, color 0.15s;
}

.app-mode-btn.active {
  background: var(--clr-card-bg);
  color: var(--clr-primary);
  box-shadow: 0 1px 4px rgba(0, 0, 0, 0.1);
}

.app-mode-hint {
  font-size: 11px;
  color: var(--clr-text-muted);
  margin: 0;
}
```

- [ ] **Step 3: 빌드 오류 확인**

```bash
npm run build 2>&1 | grep -E "error|Error" | head -20
```

Expected: 오류 없음 (컴포넌트가 아직 App.tsx에 연결되지 않아도 됨)

- [ ] **Step 4: 커밋**

```bash
git add src/features/mode/AppModeToggle.tsx src/styles.css
git commit -m "feat: add AppModeToggle component"
```

---

## Task 3: `BottomTabBar` 컴포넌트

**Files:**
- Create: `src/features/nav/BottomTabBar.tsx`
- Modify: `src/styles.css` (`.bottom-tab-bar` CSS 추가)

탭 타입은 `"edit" | "roster" | "account"` — 뷰어(`"view"`)는 앱 모드로 분리되었으므로 탭에서 제외.

- [ ] **Step 1: 컴포넌트 작성**

```tsx
// src/features/nav/BottomTabBar.tsx
export type MobileTab = "edit" | "roster" | "account";

type BottomTabBarProps = {
  activeTab: MobileTab;
  onTabChange: (tab: MobileTab) => void;
};

function IconReport() {
  return (
    <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
      <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
      <polyline points="14 2 14 8 20 8" />
      <line x1="16" y1="13" x2="8" y2="13" />
      <line x1="16" y1="17" x2="8" y2="17" />
      <polyline points="10 9 9 9 8 9" />
    </svg>
  );
}

function IconPeople() {
  return (
    <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
      <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
      <circle cx="9" cy="7" r="4" />
      <path d="M23 21v-2a4 4 0 0 0-3-3.87" />
      <path d="M16 3.13a4 4 0 0 1 0 7.75" />
    </svg>
  );
}

function IconSettings() {
  return (
    <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
      <circle cx="12" cy="12" r="3" />
      <path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 2.83-2.83l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z" />
    </svg>
  );
}

const TABS: { id: MobileTab; label: string; icon: React.ReactNode }[] = [
  { id: "edit", label: "보고서", icon: <IconReport /> },
  { id: "roster", label: "명단", icon: <IconPeople /> },
  { id: "account", label: "계정", icon: <IconSettings /> },
];

export function BottomTabBar({ activeTab, onTabChange }: BottomTabBarProps) {
  return (
    <nav className="bottom-tab-bar" aria-label="하단 탭 메뉴">
      {TABS.map((tab) => (
        <button
          key={tab.id}
          type="button"
          className={`bottom-tab${activeTab === tab.id ? " active" : ""}`}
          aria-pressed={activeTab === tab.id}
          onClick={() => onTabChange(tab.id)}
        >
          <span className="bottom-tab-icon">{tab.icon}</span>
          <span className="bottom-tab-label">{tab.label}</span>
        </button>
      ))}
    </nav>
  );
}
```

- [ ] **Step 2: CSS 추가** — `src/styles.css` 끝에 추가

```css
/* ── BottomTabBar (모바일 전용) ── */
.bottom-tab-bar {
  display: none; /* 데스크톱에서 숨김 */
}

@media (max-width: 820px) {
  .bottom-tab-bar {
    display: flex;
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    background: var(--clr-card-bg);
    border-top: 1px solid var(--clr-border);
    box-shadow: 0 -2px 12px rgba(0, 0, 0, 0.06);
    padding-bottom: env(safe-area-inset-bottom);
    z-index: 100;
  }

  .bottom-tab {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 3px;
    padding: 10px 4px 8px;
    background: transparent;
    color: var(--clr-text-muted);
    font-weight: 500;
    border-radius: 0;
  }

  .bottom-tab.active {
    color: var(--clr-primary);
  }

  .bottom-tab-icon {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .bottom-tab-label {
    font-size: 10px;
    font-weight: 600;
  }

  /* 하단 탭바 공간 확보 */
  .app-shell {
    padding-bottom: calc(70px + env(safe-area-inset-bottom));
  }
}
```

- [ ] **Step 3: 빌드 오류 확인**

```bash
npm run build 2>&1 | grep -E "error|Error" | head -20
```

Expected: 오류 없음

- [ ] **Step 4: 커밋**

```bash
git add src/features/nav/BottomTabBar.tsx src/styles.css
git commit -m "feat: add BottomTabBar component with mobile-only CSS"
```

---

## Task 4: 모바일 헤더 + 기존 미디어쿼리 개편

**Files:**
- Modify: `src/styles.css` — 모바일 `@media (max-width: 820px)` 섹션 전면 교체

현재 `@media (max-width: 820px)` 블록의 `.top-bar` 규칙을 그라데이션 헤더로 교체하고, `.segmented-control`을 모바일에서 숨긴다. 카드 스타일(그림자, border-radius)을 모바일에 적용한다.

- [ ] **Step 1: 현재 모바일 미디어쿼리 위치 확인**

```bash
grep -n "@media (max-width: 820px)" "/Users/kimwoojung/Documents/New project/Projects/ministry-report-v2/src/styles.css"
```

Expected: 1360번대 줄

- [ ] **Step 2: 기존 `@media (max-width: 820px)` 블록을 아래 내용으로 교체**

`src/styles.css`의 `@media (max-width: 820px) { ... }` 전체 블록을 찾아 아래 내용으로 교체한다:

```css
@media (max-width: 820px) {
  /* 앱 쉘 */
  .app-shell {
    padding: 0;
  }

  /* 상단바: 그라데이션 헤더 */
  .top-bar {
    background: linear-gradient(135deg, var(--clr-primary) 0%, color-mix(in srgb, var(--clr-primary) 70%, #000) 100%);
    border-radius: 0;
    flex-direction: column;
    align-items: stretch;
    margin: 0;
    padding: 16px 16px 0;
    gap: 0;
    position: sticky;
    top: 0;
    z-index: 50;
  }

  /* 헤더 타이틀 행 */
  .top-bar-title-row {
    justify-content: space-between;
    margin-bottom: 12px;
  }

  .top-bar h1 {
    font-size: 18px;
    font-weight: 800;
    letter-spacing: -0.4px;
  }

  /* 세그먼트 컨트롤: 모바일에서 숨김 (하단 탭바로 대체) */
  .segmented-control {
    display: none;
  }

  /* ThemeSelector: 헤더에서 숨김 → 계정 탭으로 이동 */
  .top-bar .theme-selector {
    display: none;
  }

  /* 콘텐츠 영역 패딩 */
  .report-mode,
  .report-meta,
  .department-list,
  .memo-section {
    grid-template-columns: 1fr;
  }

  .edit-panel {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .account-panel,
  .admin-manager,
  .history-panel,
  .import-panel {
    grid-column: 1 / -1;
    max-width: none;
  }

  .viewer-stats-grid {
    grid-template-columns: 1fr;
  }

  /* 출석 카드: 10열 → 6열 */
  .attendance-card-list {
    grid-template-columns: repeat(6, minmax(0, 1fr));
  }

  .attendance-card {
    font-size: 13px;
    padding: 5px 2px;
  }

  .info-row {
    grid-template-columns: 1fr;
  }

  /* 카드 스타일: 모바일에서 그림자 + 둥근 모서리 강조 */
  .report-form,
  .account-panel,
  .history-panel,
  .import-panel {
    border-radius: 12px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.07);
    border: none;
  }

  /* 콘텐츠 영역 패딩 (탭바 공간은 app-shell에서 처리) */
  .editor-workspace,
  .roster-shell,
  .report-mode {
    padding: 12px;
  }
}
```

- [ ] **Step 3: `@media (max-width: 500px)` 블록도 업데이트**

기존 `@media (max-width: 500px)` 블록에서 `.app-shell padding` 및 `.top-bar` 규칙을 삭제하고, 출석 카드 관련 규칙만 남긴다:

```css
@media (max-width: 500px) {
  /* 출석 카드: 5열 */
  .attendance-card-list {
    grid-template-columns: repeat(5, minmax(0, 1fr));
    gap: 4px;
  }

  .attendance-card {
    font-size: 12px;
    padding: 5px 1px;
    min-height: 30px;
  }

  .top-bar h1 {
    font-size: 16px;
  }

  .dstats-counts {
    flex-wrap: wrap;
    gap: 12px;
  }

  .report-meta {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}
```

- [ ] **Step 4: 빌드 + 개발 서버 시각 확인**

```bash
npm run build 2>&1 | grep -E "error|Error" | head -20
```

브라우저 창 너비를 820px 이하로 줄여 헤더 그라데이션, 세그먼트 컨트롤 숨김 확인.

- [ ] **Step 5: 커밋**

```bash
git add src/styles.css
git commit -m "style: mobile header gradient and card style overhaul"
```

---

## Task 5: 로그인 화면 리디자인 (`AuthGate`)

**Files:**
- Modify: `src/features/auth/AuthGate.tsx`
- Modify: `src/styles.css` (`.auth-gate` CSS 교체)

현재의 `.auth-gate` / `.auth-gate-panel` 구조를 헤더 배너 + 플로팅 카드 레이아웃으로 교체.

- [ ] **Step 1: `AuthGate.tsx` 교체**

```tsx
// src/features/auth/AuthGate.tsx
import type { Account } from "../../auth/authTypes";
import { GoogleSignInButton } from "./GoogleSignInButton";

type AuthGateProps = {
  onCreated: (account: Account) => void;
  onSignedIn: (account: Account) => void;
};

export function AuthGate({ onSignedIn }: AuthGateProps) {
  return (
    <div className="auth-gate-v2">
      <div className="auth-gate-banner">
        <div className="auth-gate-banner-icon">⛪</div>
        <h1 className="auth-gate-title">사역보고서</h1>
        <p className="auth-gate-subtitle">주일 사역 출결 · 현황 보고</p>
      </div>
      <div className="auth-gate-card-wrapper">
        <div className="auth-gate-card">
          <p className="auth-gate-card-heading">시작하기</p>
          <p className="auth-gate-card-desc">Google 계정으로 로그인하세요</p>
          <GoogleSignInButton onSignedIn={onSignedIn} />
          <p className="auth-gate-card-hint">첫 번째 로그인 계정이 관리자가 됩니다</p>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 기존 `.auth-gate` CSS를 `src/styles.css`에서 찾아 아래 내용으로 교체**

기존 `.auth-gate`, `.auth-gate-panel`, `.auth-gate-badge`, `.auth-gate-copy` 규칙을 삭제하고 아래를 추가:

```css
/* ── Auth Gate v2 (로그인 화면) ── */
.auth-gate-v2 {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  background: var(--clr-bg);
}

.auth-gate-banner {
  background: linear-gradient(135deg, var(--clr-primary) 0%, color-mix(in srgb, var(--clr-primary) 70%, #000) 100%);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 56px 24px 64px;
  text-align: center;
}

.auth-gate-banner-icon {
  font-size: 48px;
  margin-bottom: 16px;
  line-height: 1;
}

.auth-gate-title {
  font-size: 28px;
  font-weight: 800;
  color: #fff;
  margin: 0 0 8px;
  letter-spacing: -0.5px;
}

.auth-gate-subtitle {
  font-size: 14px;
  color: rgba(255, 255, 255, 0.6);
  margin: 0;
}

.auth-gate-card-wrapper {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 0 20px 40px;
  margin-top: -32px;
}

.auth-gate-card {
  background: var(--clr-card-bg);
  border-radius: 20px;
  box-shadow: 0 8px 40px rgba(0, 0, 0, 0.12);
  padding: 28px 24px;
  width: 100%;
  max-width: 380px;
  display: flex;
  flex-direction: column;
  gap: 0;
}

.auth-gate-card-heading {
  font-size: 18px;
  font-weight: 800;
  color: var(--clr-text);
  margin: 0 0 6px;
}

.auth-gate-card-desc {
  font-size: 13px;
  color: var(--clr-text-muted);
  margin: 0 0 20px;
}

.auth-gate-card-hint {
  font-size: 11px;
  color: var(--clr-text-muted);
  text-align: center;
  margin: 14px 0 0;
  opacity: 0.7;
}
```

- [ ] **Step 3: 빌드 확인**

```bash
npm run build 2>&1 | grep -E "error|Error" | head -20
```

Expected: 오류 없음

- [ ] **Step 4: 커밋**

```bash
git add src/features/auth/AuthGate.tsx src/styles.css
git commit -m "feat: redesign auth screen with banner + floating card layout"
```

---

## Task 6: `MobileReportList` 컴포넌트

**Files:**
- Create: `src/features/report/MobileReportList.tsx`
- Modify: `src/styles.css` (`.mobile-report-list` CSS 추가)

모바일 홈 화면. 보고서 목록을 카드로 표시하고, 탭하면 편집기/뷰어로 진입.

- [ ] **Step 1: 컴포넌트 작성**

```tsx
// src/features/report/MobileReportList.tsx
import type { AppMode } from "../mode/useAppMode";
import type { MinistryReport } from "../../domain/reportTypes";

type MobileReportListProps = {
  reports: MinistryReport[];
  currentReportId: string;
  appMode: AppMode;
  onSelectReport: (report: MinistryReport) => void;
  onNewReport: () => void;
};

function formatDate(dateStr: string): string {
  // dateStr: "2026-05-10"
  const [year, month, day] = dateStr.split("-").map(Number);
  const date = new Date(year, month - 1, day);
  const weekdays = ["일", "월", "화", "수", "목", "금", "토"];
  return `${year}년 ${month}월 ${day}일 (${weekdays[date.getDay()]})`;
}

function deptSummary(report: MinistryReport): string {
  const depts = [
    { label: "초등", key: "elementary" as const },
    { label: "중고등", key: "middleHigh" as const },
    { label: "청년", key: "youngAdult" as const },
    { label: "장년", key: "adult" as const },
  ];
  return depts
    .map(({ label, key }) => `${label} ${report.departments[key].attendance}`)
    .filter((_, i) => report.departments[depts[i].key].attendance > 0)
    .join(" · ");
}

export function MobileReportList({
  reports,
  appMode,
  onSelectReport,
  onNewReport,
}: MobileReportListProps) {
  const today = new Date();
  const todayStr = `${today.getFullYear()}년 ${today.getMonth() + 1}월 ${today.getDate()}일`;

  return (
    <div className="mobile-report-list">
      {appMode === "reporter" && (
        <button
          type="button"
          className="mobile-report-new-btn"
          onClick={onNewReport}
        >
          <span className="mobile-report-new-text">
            <span className="mobile-report-new-title">+ 새 보고서 작성</span>
            <span className="mobile-report-new-date">{todayStr}</span>
          </span>
          <span className="mobile-report-new-icon" aria-hidden>✏️</span>
        </button>
      )}

      {reports.length > 0 && (
        <>
          <p className="mobile-report-section-label">이전 보고서</p>
          <div className="mobile-report-card-list">
            {reports.map((r) => (
              <button
                key={r.id}
                type="button"
                className="mobile-report-card"
                onClick={() => onSelectReport(r)}
              >
                <div className="mobile-report-card-body">
                  <span className="mobile-report-card-date">{formatDate(r.reportDate)}</span>
                  <span className="mobile-report-card-summary">{deptSummary(r)}</span>
                </div>
                <span className="mobile-report-card-chevron" aria-hidden>›</span>
              </button>
            ))}
          </div>
        </>
      )}

      {reports.length === 0 && appMode === "viewer" && (
        <p className="mobile-report-empty">저장된 보고서가 없습니다</p>
      )}
    </div>
  );
}
```

- [ ] **Step 2: CSS 추가** — `src/styles.css` 끝에 추가

```css
/* ── MobileReportList ── */
.mobile-report-list {
  display: flex;
  flex-direction: column;
  gap: 8px;
  padding: 12px;
}

.mobile-report-new-btn {
  background: linear-gradient(135deg, var(--clr-primary) 0%, color-mix(in srgb, var(--clr-primary) 80%, #009966) 100%);
  border-radius: 14px;
  padding: 16px 18px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  box-shadow: 0 4px 16px rgba(var(--clr-primary-rgb), 0.3);
  color: #fff;
  text-align: left;
  width: 100%;
}

.mobile-report-new-text {
  display: flex;
  flex-direction: column;
  gap: 3px;
}

.mobile-report-new-title {
  font-size: 15px;
  font-weight: 800;
  color: #fff;
}

.mobile-report-new-date {
  font-size: 11px;
  color: rgba(255, 255, 255, 0.65);
}

.mobile-report-new-icon {
  font-size: 24px;
  opacity: 0.8;
}

.mobile-report-section-label {
  font-size: 11px;
  font-weight: 700;
  color: var(--clr-text-muted);
  text-transform: uppercase;
  letter-spacing: 0.5px;
  margin: 4px 2px 0;
}

.mobile-report-card-list {
  display: flex;
  flex-direction: column;
  background: var(--clr-card-bg);
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.07);
  overflow: hidden;
}

.mobile-report-card {
  background: transparent;
  color: inherit;
  font-weight: inherit;
  border-radius: 0;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 14px 16px;
  border-bottom: 1px solid var(--clr-border-soft);
  text-align: left;
  width: 100%;
  gap: 8px;
}

.mobile-report-card:last-child {
  border-bottom: none;
}

.mobile-report-card:active {
  background: var(--clr-surface);
}

.mobile-report-card-body {
  display: flex;
  flex-direction: column;
  gap: 3px;
  flex: 1;
  min-width: 0;
}

.mobile-report-card-date {
  font-size: 13px;
  font-weight: 700;
  color: var(--clr-text);
}

.mobile-report-card-summary {
  font-size: 11px;
  color: var(--clr-text-muted);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.mobile-report-card-chevron {
  font-size: 18px;
  color: var(--clr-border);
  flex-shrink: 0;
}

.mobile-report-empty {
  text-align: center;
  color: var(--clr-text-muted);
  font-size: 14px;
  padding: 40px 16px;
}
```

- [ ] **Step 3: 빌드 확인**

```bash
npm run build 2>&1 | grep -E "error|Error" | head -20
```

Expected: 오류 없음

- [ ] **Step 4: 커밋**

```bash
git add src/features/report/MobileReportList.tsx src/styles.css
git commit -m "feat: add MobileReportList component for mobile home screen"
```

---

## Task 7: `App.tsx` — 전체 통합

**Files:**
- Modify: `src/App.tsx`
- Modify: `src/styles.css` (모바일 계정 화면, 헤더 아바타 CSS)

새 상태 추가, 하단 탭바 연결, 모바일 화면 분기 구현.

- [ ] **Step 1: import 추가**

`src/App.tsx` 상단 import 블록에 다음을 추가:

```tsx
import { useAppMode } from "./features/mode/useAppMode";
import { AppModeToggle } from "./features/mode/AppModeToggle";
import { BottomTabBar, type MobileTab } from "./features/nav/BottomTabBar";
import { MobileReportList } from "./features/report/MobileReportList";
```

- [ ] **Step 2: `App` 함수 내 상태 추가**

기존 `const [mode, setMode] = useState<"edit" | "view" | "roster">("edit");` 바로 아래에:

```tsx
const [appMode, setAppMode] = useAppMode();
const [mobileTab, setMobileTab] = useState<MobileTab>("edit");
const [mobileScreen, setMobileScreen] = useState<"list" | "editor">("list");
```

- [ ] **Step 3: `handleLoadReport` 수정** — 모바일에서 편집기로 진입

기존 `handleLoadReport` 함수 끝에 한 줄 추가:

```tsx
function handleLoadReport(storedReport: MinistryReport) {
  const upgradedReport = upgradeReportForEditor(storedReport);
  setReport(upgradedReport);
  saveReportDraft(upgradedReport);
  setSaveErrors([]);
  setSaveStatus(`${storedReport.reportDate} 보고서를 불러왔습니다.`);
  setMobileScreen("editor"); // 추가
}
```

- [ ] **Step 4: `handleNewReport` 수정**

```tsx
function handleNewReport() {
  const nextReport = createEmptyReport(new Date(), roster);
  const draft = currentAccount
    ? reportWithAccount(nextReport, currentAccount)
    : nextReport;
  setReport(draft);
  saveReportDraft(draft);
  setSaveErrors([]);
  setSaveStatus("새 보고서를 만들었습니다.");
  setMobileScreen("editor"); // 추가
}
```

- [ ] **Step 5: 헤더 JSX 교체**

기존 `<header className="top-bar">...</header>` 전체를 아래로 교체:

```tsx
<header className="top-bar">
  <div className="top-bar-title-row">
    <div className="top-bar-title-group">
      <span className="top-bar-date">{new Date().toLocaleDateString("ko-KR", { year: "numeric", month: "long", day: "numeric", weekday: "short" })}</span>
      <h1>사역보고서</h1>
    </div>
    <div className="top-bar-actions">
      {installState === "ready" && (
        <button
          type="button"
          className="btn-pwa-install"
          onClick={() => void triggerInstall()}
          aria-label="앱 설치"
        >
          📲 설치
        </button>
      )}
      {installState === "installed" && (
        <span className="pwa-installed-badge" aria-label="앱 설치됨">✓ 설치됨</span>
      )}
      <button
        type="button"
        className="btn-force-refresh"
        onClick={() => void handleForceRefresh()}
        title="앱 강제 새로고침"
        aria-label="강제 새로고침"
      >
        🔄
      </button>
      {currentAccount && (
        <div className="top-bar-avatar" title={currentAccount.displayName} aria-label={`로그인: ${currentAccount.displayName}`}>
          {currentAccount.displayName.charAt(0).toUpperCase()}
        </div>
      )}
    </div>
  </div>
  <ThemeSelector />
  <div className="segmented-control" aria-label="보기 모드">
    <button type="button" aria-pressed={mode === "edit"} onClick={() => setMode("edit")}>보고서</button>
    <button type="button" aria-pressed={mode === "view"} onClick={() => setMode("view")}>뷰어</button>
    <button type="button" aria-pressed={mode === "roster"} onClick={() => setMode("roster")}>명단관리</button>
  </div>
</header>
```

- [ ] **Step 6: 하단 탭바 + 모바일 콘텐츠 분기 추가**

기존 `</main>` 바로 앞(마지막 닫는 태그 전)에 하단 탭바 추가:

```tsx
{currentAccount && (
  <BottomTabBar
    activeTab={mobileTab}
    onTabChange={(tab) => {
      setMobileTab(tab);
      if (tab === "edit") {
        setMode("edit");
        setMobileScreen("list");
      } else if (tab === "roster") {
        setMode("roster");
      }
    }}
  />
)}
```

- [ ] **Step 7: 모바일 `"edit"` 탭 콘텐츠 분기**

기존 `mode === "edit"` 조건에 모바일 분기를 추가. 현재:

```tsx
) : mode === "edit" ? (
  <ReportEditor ... />
```

를 아래로 교체:

```tsx
) : mode === "edit" ? (
  <>
    {/* 모바일: 목록 화면 */}
    <div className="mobile-only">
      {mobileScreen === "list" ? (
        <MobileReportList
          reports={reports}
          currentReportId={report.id}
          appMode={appMode}
          onSelectReport={(r) => {
            if (appMode === "reporter") {
              handleLoadReport(r);
            } else {
              // 뷰어 모드: 해당 보고서를 report 상태로 설정 후 viewer로 전환
              const upgraded = upgradeReportForEditor(r);
              setReport(upgraded);
              setMobileScreen("editor");
            }
          }}
          onNewReport={handleNewReport}
        />
      ) : (
        /* 모바일 편집기 (뒤로가기 버튼 포함) */
        <div className="mobile-editor-screen">
          <div className="mobile-editor-back-bar">
            <button
              type="button"
              className="mobile-back-btn"
              onClick={() => setMobileScreen("list")}
            >
              ‹ 보고서 목록
            </button>
            {appMode === "viewer" && <span className="mobile-viewer-badge">뷰어 모드</span>}
          </div>
          {appMode === "reporter" ? (
            <ReportEditor
              report={report}
              reports={reports}
              accountPanel={
                <ReporterAccountPanel
                  currentAccount={currentAccount}
                  onSignOut={() => void handleSignOut()}
                />
              }
              canSave={!!currentAccount}
              importPanel={
                <LegacyImportPanel
                  warnings={importWarnings}
                  onImport={handleImport}
                  onImportError={handleImportError}
                />
              }
              historyPanel={null}
              githubPanel={
                currentAccount.role === "admin" ? <GithubSettingsPanel /> : undefined
              }
              onChange={handleReportChange}
              onNewReport={handleNewReport}
              onSave={handleSave}
              saveErrors={saveErrors}
              saveStatus={saveStatus}
              saveDisabledReason="로그인 후 저장할 수 있습니다."
            />
          ) : (
            <ReportViewer report={report} reports={reports} />
          )}
        </div>
      )}
    </div>
    {/* 데스크톱: 기존 레이아웃 */}
    <div className="desktop-only">
      <ReportEditor
        report={report}
        reports={reports}
        accountPanel={
          <ReporterAccountPanel
            currentAccount={currentAccount}
            onSignOut={() => void handleSignOut()}
          />
        }
        canSave={!!currentAccount}
        importPanel={
          <LegacyImportPanel
            warnings={importWarnings}
            onImport={handleImport}
            onImportError={handleImportError}
          />
        }
        historyPanel={
          <ReportHistoryPanel
            reports={reports}
            currentReportId={report.id}
            onDelete={handleDeleteReport}
            onDuplicate={handleDuplicateReport}
            onLoad={handleLoadReport}
          />
        }
        githubPanel={
          currentAccount.role === "admin" ? <GithubSettingsPanel /> : undefined
        }
        onChange={handleReportChange}
        onNewReport={handleNewReport}
        onSave={handleSave}
        saveErrors={saveErrors}
        saveStatus={saveStatus}
        saveDisabledReason="로그인 후 저장할 수 있습니다."
      />
    </div>
  </>
```

- [ ] **Step 8: 모바일 `"account"` 탭 렌더링 추가**

기존 `mode === "roster"` 조건 뒤, `</main>` 앞에 계정 탭 추가:

```tsx
{mobileTab === "account" && (
  <div className="mobile-account-screen mobile-only">
    <ReporterAccountPanel
      currentAccount={currentAccount}
      onSignOut={() => void handleSignOut()}
    />
    <ThemeSelector />
    <AppModeToggle appMode={appMode} onAppModeChange={setAppMode} />
  </div>
)}
```

- [ ] **Step 9: CSS 추가** — `src/styles.css` 끝에 추가

```css
/* ── 모바일/데스크톱 전용 분기 ── */
.mobile-only {
  display: none;
}
.desktop-only {
  display: block;
}

@media (max-width: 820px) {
  .mobile-only {
    display: block;
  }
  .desktop-only {
    display: none;
  }
}

/* ── 모바일 헤더 아바타 ── */
.top-bar-date {
  display: none; /* 데스크톱에서 숨김 */
  font-size: 10px;
  color: rgba(255, 255, 255, 0.55);
  margin-bottom: 2px;
}

.top-bar-title-group {
  display: flex;
  flex-direction: column;
}

.top-bar-actions {
  display: flex;
  align-items: center;
  gap: 8px;
}

.top-bar-avatar {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.2);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 14px;
  font-weight: 700;
  color: #fff;
  flex-shrink: 0;
  cursor: default;
  user-select: none;
}

@media (max-width: 820px) {
  .top-bar-date {
    display: block;
  }
}

/* ── 모바일 편집기 헤더 (뒤로가기) ── */
.mobile-editor-screen {
  display: flex;
  flex-direction: column;
}

.mobile-editor-back-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 10px 16px 6px;
  background: color-mix(in srgb, var(--clr-primary) 70%, #000);
}

.mobile-back-btn {
  background: transparent;
  color: rgba(255, 255, 255, 0.8);
  font-size: 13px;
  font-weight: 600;
  padding: 4px 0;
}

.mobile-back-btn:hover {
  color: #fff;
  background: transparent;
}

.mobile-viewer-badge {
  font-size: 11px;
  font-weight: 700;
  color: var(--clr-auth-badge);
  background: rgba(255, 255, 255, 0.1);
  padding: 3px 8px;
  border-radius: 20px;
}

/* ── 모바일 계정 화면 ── */
.mobile-account-screen {
  padding: 12px;
  display: flex;
  flex-direction: column;
  gap: 12px;
}
```

- [ ] **Step 10: `historyPanel` null 처리 확인**

`ReportEditor.tsx`에서 `historyPanel` prop이 `null`을 받을 수 있는지 확인:

```bash
grep -n "historyPanel" "/Users/kimwoojung/Documents/New project/Projects/ministry-report-v2/src/features/report/ReportEditor.tsx" | head -10
```

만약 타입이 `ReactNode` (null 허용)이면 OK. `undefined`만 허용한다면 타입에 `| null` 추가.

- [ ] **Step 11: 전체 빌드 확인**

```bash
npm run build 2>&1 | grep -E "error|Error" | head -30
```

Expected: 오류 없음 (TypeScript 타입 오류 포함)

- [ ] **Step 12: 테스트 통과 확인**

```bash
npm test
```

Expected: 기존 테스트 전부 통과 (새 로직은 localStorage 기반이라 mocking 불필요)

- [ ] **Step 13: 커밋**

```bash
git add src/App.tsx src/styles.css
git commit -m "feat: integrate BottomTabBar, MobileReportList, AppModeToggle into App"
```

---

## Task 8: 버전 범프 + 마무리

**Files:**
- Modify: `package.json`
- Modify: `vite.config.ts` (PWA manifest name 업데이트)

- [ ] **Step 1: `package.json` 버전 수정**

`"version": "0.1.0"` → `"version": "2.0.0"`

- [ ] **Step 2: PWA manifest 앱 이름 업데이트**

`vite.config.ts`의 manifest 섹션에서:

```ts
name: "사역보고서 v2",   // 유지
short_name: "사역보고서", // 유지
```

(이미 v2로 되어 있으므로 추가 변경 불필요)

- [ ] **Step 3: 최종 빌드 + 전체 테스트**

```bash
npm run build && npm test
```

Expected: 빌드 성공, 모든 테스트 통과

- [ ] **Step 4: 커밋**

```bash
git add package.json
git commit -m "chore: bump version to 2.0.0 (Mobile UI v2)"
```

---

## Self-Review 체크리스트

**Spec coverage:**
- ✅ Task 1: `useAppMode` — 스펙 §8-2 localStorage 저장
- ✅ Task 2: `AppModeToggle` — 스펙 §8-3 모드 전환 UI
- ✅ Task 3: `BottomTabBar` — 스펙 §1-1 하단 탭바
- ✅ Task 4: 모바일 CSS — 스펙 §1-2 그라데이션 헤더, 카드 스타일
- ✅ Task 5: `AuthGate` — 스펙 §3 로그인 화면 리디자인
- ✅ Task 6: `MobileReportList` — 스펙 §2 홈 = 보고서 목록
- ✅ Task 7: `App.tsx` — 스펙 §2-3 편집기 뒤로가기, §8-4 뷰어 모드
- ✅ Task 8: `package.json` — 스펙 §7 버전 2.0.0
- ✅ 데스크톱 변경 없음 — `.desktop-only` / `.mobile-only` CSS 분기

**타입 일관성:**
- `AppMode` — `useAppMode.ts`에서 export, `AppModeToggle.tsx`와 `MobileReportList.tsx`에서 import
- `MobileTab` — `BottomTabBar.tsx`에서 export, `App.tsx`에서 import
- `historyPanel={null}` — Task 7 Step 10에서 타입 확인 필수
